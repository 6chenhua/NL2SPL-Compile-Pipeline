git diff --check completed with no whitespace errors for APW3 touched files.

Anti-pattern scan:
  Allowed hits:
    - integration_call appears only in the stale enum negative test.
    - extract_child_worker appears in existing decision logic, guard tests, and current-gap APW0 lock.
    - api_consumed_span_ids appears in API exclusion view, sanitizer, validator, materializer, and tests.
    - compile_as_call_api appears in sanitizer and existing valid enum paths.
