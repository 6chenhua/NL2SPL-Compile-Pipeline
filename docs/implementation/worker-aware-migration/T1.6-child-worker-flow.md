# T1.6: WorkerIR/Renderer child-flow support

**任务 ID**: T1.6  
**任务名称**: WorkerIR/Renderer child-flow support  
**预估时间**: 2-4 天  
**前置依赖**: T1.5  
**状态**: 待启动

---

## 1. 任务概述

升级 `ChildWorkerIR` 支持 child flow + steps，修改 Stage 10 和 Stage 11 渲染 child flow。

### 目标
- 升级 `ChildWorkerIR` 支持 child flow + steps
- 修改 Stage 10 从 worker-scoped 数据组装 WorkerIR
- 修改 Stage 11 渲染 child worker 的完整 flow

### 成功标准
- `ChildWorkerIR` 能承载 child flow + steps
- Stage 10 能从 worker-scoped 数据组装 WorkerIR
- Stage 11 能渲染 child worker 的完整 flow（不是 synthetic st_child）
- 所有现有测试通过

---

## 2. 任务范围

### 可创建文件
- `tests/ir/test_child_worker_ir.py` - ChildWorkerIR 测试
- `tests/pipeline/stages/test_stage10_worker_scoped.py` - Stage 10 worker-scoped 测试
- `tests/pipeline/stages/test_stage11_child_worker_render.py` - Stage 11 child worker 渲染测试

### 可编辑文件
- `src/nl2spl/ir/worker_ir.py` - 升级 `ChildWorkerIR`
- `src/nl2spl/pipeline/stages/stage10_worker_assembler.py` - 新增 worker-aware 方法
- `src/nl2spl/pipeline/stages/stage11_spl_renderer.py` - 修改 child worker 渲染
- `src/nl2spl/pipeline/orchestrator.py` - 新增 worker-aware 调用路径

### 不可编辑文件
- IR 文件（除 `worker_ir.py` 外）
- Stage 7/9.5 文件

---

## 3. 详细工作内容

### 3.1 升级 ChildWorkerIR

**文件**: `src/nl2spl/ir/worker_ir.py`

**修改类** (D3 - Frozen):

```python
@dataclass
class ChildWorkerIR:
    """Concrete child worker with full flow and steps support.
    
    Attributes:
        worker_name: Child worker name
        description: Child worker description
        task_text: Command text for the delegated task
        inputs: Input specifications
        outputs: Output specifications
        main_flow: Main flow reference (新增)
        alternative_flows: Alternative flow references (新增)
        exception_flows: Exception flow references (新增)
        api_refs: Referenced API names (新增)
        steps: Child worker steps (新增) ← 关键：renderer 需要
    """
    worker_name: str
    description: str
    task_text: str
    inputs: list[WorkerInput] = field(default_factory=list)
    outputs: list[WorkerOutput] = field(default_factory=list)
    # 新增：支持 child flow
    main_flow: FlowRef = field(default_factory=FlowRef)
    alternative_flows: list[AlternativeFlowRef] = field(default_factory=list)
    exception_flows: list[ExceptionFlowRef] = field(default_factory=list)
    api_refs: list[str] = field(default_factory=list)
    # 新增：支持 child steps
    steps: list[StepIR] = field(default_factory=list)
```

### 3.2 修改 Stage 10 WorkerAssembler

**文件**: `src/nl2spl/pipeline/stages/stage10_worker_assembler.py`

**新增方法**:

#### 3.2.1 assemble_from_worker_scoped()

```python
def assemble_from_worker_scoped(
    self,
    worker_step_plan: WorkerStepPlanIR,
    worker_scoped_resources: WorkerScopedResourceIR | None,
    symbol_table: SymbolTable,
    worker_plan: WorkerPlanIR,
    worker_flow_plan: WorkerFlowPlanIR | None = None,
    worker_block_plan: WorkerBlockPlanIR | None = None,
) -> WorkerIR:
    """Assemble WorkerIR from worker-scoped step plan.
    
    直接从 worker_step_plan 组装，不再依赖 delegation_candidates。
    """
```

#### 3.2.2 _build_child_worker()

```python
def _build_child_worker(
    self,
    worker: WorkerSpecIR,
    steps: list[StepIR],
    flow: FlowStructureIR | None,
    blocks: BlockStructureIR | None,
    symbol_table: SymbolTable,
) -> ChildWorkerIR:
    """Build a ChildWorkerIR from worker spec, steps, flow, and blocks.
    
    Args:
        worker: Worker specification
        steps: Child worker steps
        flow: Child worker flow (if available)
        blocks: Child worker blocks (if available)
        symbol_table: Symbol table
        
    Returns:
        ChildWorkerIR with full flow and steps support
    """
```

### 3.3 修改 Stage 11 SPLRenderer

**文件**: `src/nl2spl/pipeline/stages/stage11_spl_renderer.py`

**修改方法**:

#### 3.3.1 _render_child_worker()

```python
def _render_child_worker(self, child: ChildWorkerIR, indent: int) -> list[str]:
    """Render child worker with full flow support.
    
    使用 child.main_flow.blocks 和 child.steps 渲染，
    而不是 synthetic st_child。
    """
```

#### 3.3.2 _render_block()

```python
def _render_block(
    self,
    block: BlockIR,
    steps: list[StepIR],
    indent: int,
) -> list[str]:
    """Render a block with its steps."""
```

#### 3.3.3 _render_step()

```python
def _render_step(self, step: StepIR, indent: int) -> list[str]:
    """Render a single step."""
```

### 3.4 修改 Orchestrator

**文件**: `src/nl2spl/pipeline/orchestrator.py`

**新增方法**:

```python
def _run_stage10_worker_scoped(
    self,
    worker_step_plan: WorkerStepPlanIR,
    resources: ResourceRegistryIR,
    symbol_table: SymbolTable,
    worker_plan: WorkerPlanIR,
    worker_flow_plan: WorkerFlowPlanIR | None = None,
    worker_block_plan: WorkerBlockPlanIR | None = None,
) -> WorkerIR:
    """Run Stage 10 with worker-scoped input."""
```

**修改 Stage 10 调用**:

```python
# Stage 10: Worker Assembly
if (self.config.enable_worker_boundary_planner
    and worker_step_plan is not None
    and worker_plan is not None):
    # Worker-aware path
    worker = self._run_stage10_worker_scoped(...)
else:
    # Legacy path
    worker = self._run_stage10(...)
```

### 3.5 创建测试

#### 3.5.1 ChildWorkerIR 测试

**文件**: `tests/ir/test_child_worker_ir.py`

```python
class TestChildWorkerIR:
    def test_init_with_defaults(self):
        """Test ChildWorkerIR initialization with default values."""
        
    def test_init_with_flow(self):
        """Test ChildWorkerIR initialization with flow."""
        
    def test_init_with_steps(self):
        """Test ChildWorkerIR initialization with steps."""
        
    def test_init_with_all_fields(self):
        """Test ChildWorkerIR initialization with all fields."""
```

#### 3.5.2 Stage 10 worker-scoped 测试

**文件**: `tests/pipeline/stages/test_stage10_worker_scoped.py`

```python
class TestWorkerAssemblerWorkerScoped:
    def test_assemble_from_worker_scoped_single_worker(self):
        """Test assemble_from_worker_scoped with single main worker."""
        
    def test_assemble_from_worker_scoped_multiple_workers(self):
        """Test assemble_from_worker_scoped with main + child workers."""
        
    def test_build_child_worker_with_flow(self):
        """Test _build_child_worker with flow."""
        
    def test_build_child_worker_with_steps(self):
        """Test _build_child_worker with steps."""
        
    def test_build_child_worker_with_all_fields(self):
        """Test _build_child_worker with all fields."""
```

#### 3.5.3 Stage 11 child worker 渲染测试

**文件**: `tests/pipeline/stages/test_stage11_child_worker_render.py`

```python
class TestSPLRendererChildWorker:
    def test_render_child_worker_with_flow(self):
        """Test _render_child_worker with flow."""
        
    def test_render_child_worker_with_steps(self):
        """Test _render_child_worker with steps."""
        
    def test_render_child_worker_with_alternative_flows(self):
        """Test _render_child_worker with alternative flows."""
        
    def test_render_child_worker_with_exception_flows(self):
        """Test _render_child_worker with exception flows."""
        
    def test_render_block_sequential(self):
        """Test _render_block with SEQUENTIAL block."""
        
    def test_render_block_if(self):
        """Test _render_block with IF block."""
        
    def test_render_block_for(self):
        """Test _render_block with FOR block."""
        
    def test_render_block_while(self):
        """Test _render_block with WHILE block."""
        
    def test_render_step_invoke_worker(self):
        """Test _render_step with INVOKE_WORKER step."""
        
    def test_render_step_call_api(self):
        """Test _render_step with CALL_API step."""
        
    def test_render_step_general_command(self):
        """Test _render_step with GENERAL_COMMAND step."""
```

---

## 4. 验收标准

### 4.1 代码验收
- [ ] `ChildWorkerIR` 包含新字段（main_flow, steps, alternative_flows, exception_flows, api_refs）
- [ ] `WorkerAssembler.assemble_from_worker_scoped()` 实现正确
- [ ] `WorkerAssembler._build_child_worker()` 实现正确
- [ ] `SPLRenderer._render_child_worker()` 渲染 child flow（不是 synthetic st_child）
- [ ] `SPLRenderer._render_block()` 渲染 block 正确
- [ ] `SPLRenderer._render_step()` 渲染 step 正确
- [ ] Orchestrator 正确处理 worker-aware 和 legacy path

### 4.2 测试验收
- [ ] `tests/ir/test_child_worker_ir.py` 所有测试通过
- [ ] `tests/pipeline/stages/test_stage10_worker_scoped.py` 所有测试通过
- [ ] `tests/pipeline/stages/test_stage11_child_worker_render.py` 所有测试通过
- [ ] 所有现有测试通过（回归测试）

### 4.3 集成验收
- [ ] 端到端测试：输入包含 child worker 的场景
- [ ] 验证生成的 `ChildWorkerIR` 包含正确的 flow 和 steps
- [ ] 验证 Stage 11 能正确渲染 child worker 的 flow
- [ ] 验证渲染的 SPL 包含 child worker 的完整 flow

---

## 5. 依赖关系

### 前置依赖
- T1.5: Worker-aware Stage 9.5（必须完成）

### 后续任务
- T2: WorkerScopedResourceIR + SymbolTable
- T3: worker-aware path 去 adapter

### 与其他任务的接口

#### 从 T1.5 接收的接口
- 校验通过的 `WorkerStepPlanIR`
- 校验通过的 `WorkerFlowPlanIR`
- 校验通过的 `WorkerBlockPlanIR`

#### 提供给 T2 的接口
- `ChildWorkerIR` 数据结构
- `WorkerAssembler.assemble_from_worker_scoped()` 方法

#### 提供给 T3 的接口
- `ChildWorkerIR` 数据结构
- Stage 11 渲染逻辑

---

## 6. 交付物

### 代码交付
1. 修改后的 `worker_ir.py`（升级 `ChildWorkerIR`）
2. 修改后的 `stage10_worker_assembler.py`（新增 worker-aware 方法）
3. 修改后的 `stage11_spl_renderer.py`（修改 child worker 渲染）
4. 修改后的 `orchestrator.py`（新增 worker-aware 调用路径）

### 测试交付
1. `tests/ir/test_child_worker_ir.py`
2. `tests/pipeline/stages/test_stage10_worker_scoped.py`
3. `tests/pipeline/stages/test_stage11_child_worker_render.py`

---

## 7. 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| ChildWorkerIR 改动影响现有代码 | 中 | 新增字段有默认值，不破坏兼容性 |
| Stage 11 渲染改动影响现有功能 | 高 | 保留 legacy path，通过配置开关控制 |
| SPL 语法不支持某些结构 | 中 | 参考现有 SPL 语法规范 |

---

## 8. 评审要点

### 代码评审
- `ChildWorkerIR` 新增字段是否有默认值
- `assemble_from_worker_scoped()` 是否正确组装 WorkerIR
- `_build_child_worker()` 是否正确构建 ChildWorkerIR
- `_render_child_worker()` 是否渲染 child flow（不是 synthetic st_child）
- `_render_block()` 是否正确渲染各种 block 类型
- Orchestrator 是否正确处理 worker-aware 和 legacy path

### 测试评审
- 测试覆盖是否充分
- 边界条件是否测试
- 渲染结果是否正确

---

## 9. 开发建议

### 开发顺序
1. 升级 `ChildWorkerIR`（新增字段）
2. 实现 `_build_child_worker()`
3. 实现 `assemble_from_worker_scoped()`
4. 修改 `_render_child_worker()`
5. 实现 `_render_block()` 和 `_render_step()`
6. 修改 Orchestrator
7. 编写测试
8. 运行回归测试

### 调试建议
- 使用简单的测试场景（单 child worker）
- 逐步增加复杂度（多 child worker、复杂 flow）
- 使用 logging 输出渲染结果

---

## 10. 参考资料

- [迁移方案 v3.0](../migration-worker-aware-pipeline.md)
- [ChildWorkerIR 定义](../../src/nl2spl/ir/worker_ir.py)
- [Stage 10 实现](../../src/nl2spl/pipeline/stages/stage10_worker_assembler.py)
- [Stage 11 实现](../../src/nl2spl/pipeline/stages/stage11_spl_renderer.py)
- [T1.5 任务文档](T1.5-worker-aware-normalizer.md)
