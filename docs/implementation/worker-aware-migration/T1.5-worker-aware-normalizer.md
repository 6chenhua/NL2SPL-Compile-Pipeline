# T1.5: Worker-aware Stage 9.5

**任务 ID**: T1.5  
**任务名称**: Worker-aware Stage 9.5  
**预估时间**: 2-3 天  
**前置依赖**: T1  
**状态**: 待启动

---

## 1. 任务概述

在 Stage 10 之前进行 worker-scoped normalization，确保 IR 完整性。

### 目标
- 实现 `normalize_worker_scoped()` 方法
- 验证 span ownership（D5: error）
- 验证 handoff completeness
- 验证 output binding
- 验证 producer/consumer reachability

### 成功标准
- Stage 9.5 能校验 worker-scoped IR 的完整性
- span ownership violation 抛出 error
- 所有现有测试通过

---

## 2. 任务范围

### 可创建文件
- `tests/pipeline/stages/test_stage9_5_worker_scoped.py` - Stage 9.5 worker-scoped 测试

### 可编辑文件
- `src/nl2spl/pipeline/stages/stage9_5_normalizer.py` - 新增 worker-aware 方法
- `src/nl2spl/pipeline/orchestrator.py` - 新增 worker-aware 调用路径

### 不可编辑文件
- IR 文件
- 其他 Stage 实现文件
- Stage 7/10/11 文件

---

## 3. 详细工作内容

### 3.1 修改 Stage 9.5 IRNormalizer

**文件**: `src/nl2spl/pipeline/stages/stage9_5_normalizer.py`

**新增方法**:

#### 3.1.1 normalize_worker_scoped()

```python
def normalize_worker_scoped(
    self,
    worker_flow_plan: WorkerFlowPlanIR,
    worker_block_plan: WorkerBlockPlanIR,
    worker_step_plan: WorkerStepPlanIR,
    worker_plan: WorkerPlanIR,
    resources: ResourceRegistryIR,
    symbol_table: SymbolTable,
) -> tuple[
    WorkerFlowPlanIR,
    WorkerBlockPlanIR,
    WorkerStepPlanIR,
    SymbolTable,
    list[str],  # errors
    list[str],  # warnings
]:
    """Normalize and validate worker-scoped IRs.
    
    校验内容（D5: span ownership violation 是 error）：
    1. main worker steps 不引用 child-owned spans => error
    2. child worker steps 只引用自己 owned spans => error
    3. handoff step 存在且绑定完整 => error/warning
    4. child output 是否被 parent 消费或声明为 final => error
    5. worker-local producer/consumer reachability => warning
    6. CALL_API handoff 和 INVOKE_WORKER handoff 分开校验 => error
    """
```

#### 3.1.2 _validate_span_ownership()

```python
def _validate_span_ownership(
    self,
    worker_step_plan: WorkerStepPlanIR,
    worker_plan: WorkerPlanIR,
) -> list[str]:
    """Validate span ownership across workers.
    
    D5: span ownership violation 是 error，不是 warning。
    
    Rules:
    - Non-handoff step references span outside owner worker => error
    - Main ordinary step references child-owned span => error
    - Child ordinary step references parent-owned span => error
    - Handoff step source_span_ids must be caller-owned or empty-with-warning
    """
```

#### 3.1.3 _validate_handoffs()

```python
def _validate_handoffs(
    self,
    worker_step_plan: WorkerStepPlanIR,
    worker_plan: WorkerPlanIR,
) -> tuple[list[str], list[str]]:
    """Validate handoff completeness."""
```

#### 3.1.4 _validate_output_binding()

```python
def _validate_output_binding(
    self,
    worker_step_plan: WorkerStepPlanIR,
    worker_plan: WorkerPlanIR,
    symbol_table: SymbolTable,
) -> list[str]:
    """Validate child output is consumed by parent or declared as final."""
```

#### 3.1.5 _validate_reachability()

```python
def _validate_reachability(
    self,
    worker_step_plan: WorkerStepPlanIR,
    worker_plan: WorkerPlanIR,
    symbol_table: SymbolTable,
) -> list[str]:
    """Validate worker-local producer/consumer reachability."""
```

#### 3.1.6 _validate_handoff_types()

```python
def _validate_handoff_types(
    self,
    worker_plan: WorkerPlanIR,
) -> list[str]:
    """Validate CALL_API and INVOKE_WORKER handoffs separately."""
```

### 3.2 修改 Orchestrator

**文件**: `src/nl2spl/pipeline/orchestrator.py`

**新增方法**:

```python
def _run_normalization_worker_scoped(
    self,
    worker_flow_plan: WorkerFlowPlanIR,
    worker_block_plan: WorkerBlockPlanIR,
    worker_step_plan: WorkerStepPlanIR,
    worker_plan: WorkerPlanIR,
    resources: ResourceRegistryIR,
    symbol_table: SymbolTable,
) -> tuple[
    WorkerFlowPlanIR,
    WorkerBlockPlanIR,
    WorkerStepPlanIR,
    SymbolTable,
    list[str],
    list[str],
]:
    """Run worker-scoped normalization."""
```

**修改 Stage 9.5 调用**:

```python
# Stage 9.5: IR Normalization
if (self.config.enable_worker_boundary_planner
    and worker_flow_plan is not None
    and worker_block_plan is not None
    and worker_step_plan is not None
    and worker_plan is not None):
    # Worker-aware path
    norm_result = self._run_normalization_worker_scoped(...)
    ...
    # D5: span ownership violation 是 error
    if normalization_errors:
        raise ValueError("Worker-scoped normalization failed: ...")
else:
    # Legacy path
    norm_result = self._run_normalization(...)
```

### 3.3 创建测试

**文件**: `tests/pipeline/stages/test_stage9_5_worker_scoped.py`

```python
class TestIRNormalizerWorkerScoped:
    def test_normalize_worker_scoped_success(self):
        """Test normalize_worker_scoped with valid input."""
        
    def test_normalize_worker_scoped_span_ownership_error(self):
        """Test normalize_worker_scoped with span ownership violation (D5: error)."""
        
    def test_validate_span_ownership_main_references_child(self):
        """Test _validate_span_ownership when main step references child span."""
        
    def test_validate_span_ownership_child_references_parent(self):
        """Test _validate_span_ownership when child step references parent span."""
        
    def test_validate_span_ownership_valid(self):
        """Test _validate_span_ownership with valid spans."""
        
    def test_validate_handoffs_missing_step(self):
        """Test _validate_handoffs when handoff has no corresponding step."""
        
    def test_validate_handoffs_missing_binding(self):
        """Test _validate_handoffs when handoff has no input/output bindings."""
        
    def test_validate_handoffs_valid(self):
        """Test _validate_handoffs with valid handoffs."""
        
    def test_validate_output_binding_unbound(self):
        """Test _validate_output_binding when child output is not bound."""
        
    def test_validate_output_binding_bound(self):
        """Test _validate_output_binding when child output is bound."""
        
    def test_validate_reachability_missing_producer(self):
        """Test _validate_reachability when variable has no producer."""
        
    def test_validate_reachability_valid(self):
        """Test _validate_reachability with valid reachability."""
        
    def test_validate_handoff_types_invoke_missing_worker(self):
        """Test _validate_handoff_types when INVOKE handoff missing to_worker."""
        
    def test_validate_handoff_types_api_call_missing_ref(self):
        """Test _validate_handoff_types when API_CALL handoff missing api_ref."""
        
    def test_validate_handoff_types_valid(self):
        """Test _validate_handoff_types with valid handoffs."""
```

---

## 4. 验收标准

### 4.1 代码验收
- [ ] `IRNormalizer.normalize_worker_scoped()` 实现正确
- [ ] `_validate_span_ownership()` 抛出 error（D5）
- [ ] `_validate_handoffs()` 验证 completeness
- [ ] `_validate_output_binding()` 验证 child output binding
- [ ] `_validate_reachability()` 验证 producer/consumer reachability
- [ ] `_validate_handoff_types()` 分离 invoke/api_call 校验
- [ ] Orchestrator 正确处理 worker-aware 和 legacy path

### 4.2 测试验收
- [ ] `tests/pipeline/stages/test_stage9_5_worker_scoped.py` 所有测试通过
- [ ] 所有现有测试通过（回归测试）

### 4.3 集成验收
- [ ] 端到端测试：输入包含 child worker 的场景
- [ ] 验证 span ownership violation 抛出 error
- [ ] 验证 handoff completeness 校验正确
- [ ] 验证 output binding 校验正确

---

## 5. 依赖关系

### 前置依赖
- T1: WorkerScopedStepIR + Stage 7（必须完成）

### 后续任务
- T1.6: WorkerIR/Renderer child-flow support
- T2: WorkerScopedResourceIR + SymbolTable

### 与其他任务的接口

#### 从 T1 接收的接口
- `WorkerStepPlanIR` 数据结构
- `StepExtractor.execute_worker_scoped()` 方法

#### 提供给 T1.6 的接口
- 校验通过的 `WorkerStepPlanIR`
- 校验通过的 `WorkerFlowPlanIR`
- 校验通过的 `WorkerBlockPlanIR`

---

## 6. 交付物

### 代码交付
1. 修改后的 `stage9_5_normalizer.py`（新增 worker-aware 方法）
2. 修改后的 `orchestrator.py`（新增 worker-aware 调用路径）

### 测试交付
1. `tests/pipeline/stages/test_stage9_5_worker_scoped.py`

---

## 7. 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 校验规则过严 | 中 | 可调整 warning/error 级别 |
| 校验规则过松 | 中 | 参考 D5 决策，span ownership 必须是 error |
| 性能问题 | 低 | 校验逻辑简单，性能影响小 |

---

## 8. 评审要点

### 代码评审
- `normalize_worker_scoped()` 是否正确调用所有校验方法
- `_validate_span_ownership()` 是否抛出 error（D5）
- `_validate_handoffs()` 是否检查 completeness
- `_validate_output_binding()` 是否检查 child output binding
- Orchestrator 是否正确处理 error

### 测试评审
- 测试覆盖是否充分
- 边界条件是否测试
- 错误场景是否测试

---

## 9. 开发建议

### 开发顺序
1. 实现 `_validate_span_ownership()`（核心校验）
2. 实现 `_validate_handoffs()`
3. 实现 `_validate_output_binding()`
4. 实现 `_validate_reachability()`
5. 实现 `_validate_handoff_types()`
6. 实现 `normalize_worker_scoped()`
7. 修改 Orchestrator
8. 编写测试
9. 运行回归测试

### 调试建议
- 使用简单的测试场景（单 worker）
- 逐步增加复杂度（多 worker、复杂 handoff）
- 使用 logging 输出校验结果

---

## 10. 参考资料

- [迁移方案 v3.0](../migration-worker-aware-pipeline.md)
- [Stage 9.5 实现](../../src/nl2spl/pipeline/stages/stage9_5_normalizer.py)
- [Orchestrator 实现](../../src/nl2spl/pipeline/orchestrator.py)
- [T1 任务文档](T1-worker-scoped-step.md)
